---
name: 'Lint'
on:
  pull_request: {}
  push:
    branches:
      - 'master'
  schedule:
    - cron: '0 0 * * *'
jobs:
  python-lint:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v2'
      - uses: 'ricardochaves/python-lint@v1.4.0'
        with:
          python-root-list: >-
            custom_components/unavailable_entities
            tests

          # TODO: Enable these linters.
          use-pylint: false
          use-pycodestyle: false
          use-mypy: false
  yamllint:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v2'
      - uses: 'ibiqlik/action-yamllint@v3'
